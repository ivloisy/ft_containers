#include <iostream>
#include <string>
#include <deque>
#include "includes/vector.hpp"

int main()
{
	ft::vector<int>	vector;
	ft::vector<int> vector2(vector);
	// int* arr = vector.getAlloc().allocate(5);

	// arr[3] = 10;

	// std::cout << arr[3] << std::endl;
	// vector.getAlloc().deallocate(arr, 5);
	return 0;
}